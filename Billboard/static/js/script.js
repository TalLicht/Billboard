Board = {};Board.init = function(){    $(document).ready(function(){        $("#newPost").hide();        $("#submit").hide();        $("#plusBtn").bind("click",Board.addNewPost);    })};function getCookie(name) {    var cookieValue = null;    if (document.cookie && document.cookie !== '') {        var cookies = document.cookie.split(';');        for (var i = 0; i < cookies.length; i++) {            var cookie = jQuery.trim(cookies[i]);            if (cookie.substring(0, name.length + 1) === (name + '=')) {                cookieValue = decodeURIComponent(cookie.substring(name.length + 1));                break;            }        }    }return cookieValue;};var csrftoken = getCookie('csrftoken');function csrfSafeMethod(method) {    return (/^(GET|HEAD|OPTIONS|TRACE)$/.test(method));};$.ajaxSetup({    beforeSend: function(xhr, settings) {        if (!csrfSafeMethod(settings.type) && !this.crossDomain) {            xhr.setRequestHeader("X-CSRFToken", csrftoken);        }    }});Board.formatDate = function(date){    var day = date.getDate();    var month = date.getMonth() + 1;    if (month<=9){        month = '0' + String(month)    }    var year = date.getFullYear();    var finalDate = day + "/" + month + "/" + year;    return finalDate}Board.addNewPost = function(){    $("#newPost").slideDown();    $("#submit").slideDown();    $("#addPostBtn").slideUp();    $("#noPost").hide();    $("#newPostDate").text(Board.formatDate(new Date));    $("#confirmbtn").bind("click",Board.createPost);    $("#closebtn").bind("click",Board.cancelPost);};Board.createPost = function(){    $("#newPost").slideUp();    $("#submit").slideUp();    $("#addPostBtn").slideUp();    $.ajax("newpost",{    type: "POST",    data:{          "title":$("#newPostTitle").val(),          "text":$("#newPostText").val(),          "author":$("#newPostAuthor").val()    },    success: function(data)    {        $("#row").html(data + $("#row").html())        $("#newPostText").val(""),        $("#newPostTitle").val(""),        $("#newPostAuthor").val("")    }});};Board.cancelPost = function(){    $("#newPost").slideUp();    $("#submit").slideUp();    $("#addPostBtn").slideDown();    $("#newPostText").val(""),    $("#newPostTitle").val(""),    $("#newPostAuthor").val("")};Board.init();